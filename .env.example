# Resend API key for the website contact / project-request forms.
# Get one at https://resend.com/api-keys and set the same value in
# Vercel → Project → Settings → Environment Variables.
RESEND_API_KEY=
